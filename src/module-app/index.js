import React from "react";
import ReactDOM from "react-dom";

import { ModuleApp } from "./ModuleApp";

const rootElement = document.getElementById("module-root");
ReactDOM.render(<ModuleApp />, rootElement);
