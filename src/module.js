import React from "react";
import ReactDOM from "react-dom";

import FrameBody from "./FrameBody";

const rootElement = document.getElementById("module");
ReactDOM.render(<FrameBody />, rootElement);
